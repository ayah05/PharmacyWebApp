wichtig:
============================================
der inhalt von output und upload kann gelöscht werden, die ordner selbst müssen jedoch im verzeichnis bleiben.

wenn es trotzdem nicht geht, php.ini suchen (zB <code>C:\xampp\php\php.ini</code>) und dort <code>file_uploads=On</code> setzen.
